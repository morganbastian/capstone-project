import { TextField, Button, Paper } from "@mui/material"

//** Setup (define helper functions and variables here)

function Home(props) {
    //** Destructure Props
    const {
  
    } = props
  
    //** State Variables
  
    //** Component Logic
  
    //** Return JSX
    return (
        <Paper>
          'hello'
        </Paper>
      )
    }
export default Home